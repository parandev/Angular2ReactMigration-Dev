
# Create a Docker image and push it to Artifactory

# This workflow uses actions that are not certified by GitHub.
# They are provided by a third-party and are governed by
# separate terms of service, privacy policy, and support
# documentation.

# GitHub recommends pinning actions to a commit SHA.
# To get a newer version, you will need to update the SHA.
# You can also reference a tag or branch, but the action may change without warning.

name: Build and Publish Docker image

on:
  release:
    types: [published]

jobs:
  push_to_registry:
    name: Push Docker image to Artifactory
    runs-on:  gha-runner-scale-set-linux
    # permissions:
    #   packages: write
    #   contents: read
    #   attestations: write
    #   id-token: write
    # steps:
    #   - name: Import Secrets
    #     uses: hashicorp/vault-action@v3
    #     with:
    #       url: https://vault-automated.dot.ga.gov
    #       method: approle
    #       tlsSkipVerify: true
    #       roleId: ${{ secrets.VAULT_ROLE_ID }}
    #       secretId: ${{ secrets.VAULT_SECRET_ID }}
    #       secrets: |
    #         linux_devops_kv/data/artifactory password | ARTIFACTORY_TOKEN ;
    #         linux_devops_kv/data/artifactory username | ARTIFACTORY_USERNAME ;
    #         linux_devops_kv/data/artifactory url | ARTIFACTORY_URL ;

    #   - name: Check out the repo
    #     uses: actions/checkout@v4

    #   - name: Docker meta
    #     id: meta
    #     uses: docker/metadata-action@v5
    #     with:
    #       images: |
    #         artifactory.dot.ga.gov/gdot-docker/tide
    #       tags: |
    #         type=semver,pattern={{version}}

    #   - name: Login to DockerHub
    #     uses: docker/login-action@v3
    #     with:
    #       registry: ${{ env.ARTIFACTORY_URL }}
    #       username: ${{ env.ARTIFACTORY_USERNAME }}
    #       password: ${{ env.ARTIFACTORY_TOKEN }}

    #   - name: Build and push
    #     uses: docker/build-push-action@v6
    #     with:
    #       context: .
    #       build-args: |
    #         APPDIR=/opt/tide
    #         VERSION=${{ github.event.release.tag_name }}
    #       push: true
    #       tags: ${{ steps.meta.outputs.tags }}
    #       labels: ${{ steps.meta.outputs.labels }}
